# 执行计划: 底盘控制架构硬化与协议单义化

## 内部执行级别

- `L`: 串行执行，改动跨多个模块但依赖明确

## 计划

1. 修改底盘模式策略与控制仲裁
2. 将运行时命令状态改为单义字段
3. 扩展 binary / ASCII 状态回报
4. 修改 ESP01S 状态解析、模式确认与 UI 文案
5. 更新协议文档
6. 做静态核对

## 验证

- `rg` 检查关键新字段和模式门禁
- 人工审查协议文档与代码是否一致
- 不做本地编译，因为当前环境缺少 `cmake`
